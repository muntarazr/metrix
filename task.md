# METRIX — سجل مهام الجاهزية للإنتاج

> هذا ملف عمل حيّ. يجب تحديثه مع كل تغيير تقني: الحالة، الملفات المتأثرة، وطريقة التحقق.

## الحالة العامة

- **المرحلة الحالية:** Private Beta / Pre-production — بعد تقدم ملحوظ في جاهزية الكود
- **قرار الإطلاق العام:** قيد التحقق — يتطلب تطبيق مهاجرتين يدويتين على Supabase الحي ومراجعة قانونية قبل الإطلاق الفعلي (انظر قسم المخاطر المفتوحة)
- **آخر تحديث:** 2026-09-04
- **التقييم:** تم إنجاز معالجة حقيقية لجميع مهمات P0 وP1 الممكنة بالكود في هذه الجلسة: تدقيق API وإصلاح ثلاث ثقوب تفويض حقيقية، إضافة نظام quota ذري لـ AI، تحصين رفع الصوت، معالجة سباقات تحديات وتقوية رمز الدعوة، تأكيد نجاح Production build فعليًا، إضافة 27 اختبارًا للمنطق الحساس بلا dependencies تحتاج npm install، إنشاء مسودة خصوصية/شروط، وتحديث الوثائق. المتبقي الرئيسي هو النشر اليدوي لمهاجرتي `0007` و`0008` على Supabase، واختبار بشري للويب والموبايل، ومراجعة قانونية لنصوص الخصوصية.

## قواعد تحديث هذا الملف

عند تنفيذ أي مهمة:

1. انقلها من `قيد التنفيذ` إلى `مكتملة` أو حدّث حالتها.
2. اذكر الملفات التي تغيرت.
3. اذكر التحقق الذي تم فعليًا ونتيجته.
4. لا تُعلّم المهمة مكتملة إذا لم يتم التحقق منها.
5. سجّل أي عائق أو مخاطرة متبقية في قسم `المخاطر والقرارات المفتوحة`.

---

## P0 — مانعات الإطلاق العام (Blockers)

### 1) تدقيق حماية API والتفويض على الموارد
- **الحالة:** المراجعة الساكنة لجميع مسارات API والإصلاحات المكتشفة **تمّت بنجاح** في الكود — 2026-09-04 (انظر المهام 8.1، 8.2، 1.1، 1.2 أدناه). **ملاحظة:** الاختبار التكاملي بحسابين حقيقيين على بيئة منشورة لم يُنفّذ (يتطلب حسابين حقيقيين وبيئة منشورة)، فبقي هذا البند مفتوحًا رمزيًا حتى يتم تنفيذه.
- **الهدف:** كل مسار API يتحقق من هوية المستخدم وصلاحيته على الـ goal/task/challenge المطلوب، في الويب والموبايل.
- **النطاق:** `src/app/api/**/route.ts`، `src/lib/api-auth.ts`، `src/utils/supabase/server-request.ts`
- **معايير القبول:**
  - جميع المسارات تتطلب مستخدمًا مصادقًا عليه، باستثناء أي مسار عام موثق صراحة.
  - لا يستطيع User B قراءة أو تعديل موارد User A عبر تغيير `goalId` أو `taskId` أو `logId` أو `challengeId`.
  - تعمل المصادقة مع Cookies للويب وBearer token للموبايل.
- **التحقق المطلوب:** اختبار يدوي بحسابين + مراجعة routes.

### 2) حماية تكلفة وإساءة استخدام خدمات AI
- **الحالة:** التنفيذ البرمجي تمّ بنجاح (الحجز الذري 2.1، حدود JSON 1.1، حماية الصوت). المتبقي الوتيد الوحيد: تطبيق migration `0007` فعليًا على Supabase الحي واتتباع رسالتي `503`/`429` من حساب حقيقي.
- **الهدف:** منع الاستدعاءات غير المحدودة لـ Gemini وMistral وتوليد الصور.
- **النطاق:** routes الخاصة بـ plan/evaluate/investigate/ai-edit/daily-focus/weekly-review/task-mini/milestone image/transcribe.
- **معايير القبول:**
  - Rate limit لكل مستخدم ولكل نوع عملية.
  - تحديد طول النص، عدد الطلبات، وحجم/مدة ملف الصوت.
  - منع النقر أو الإرسال المكرر من الواجهة.
  - رسائل ثنائية اللغة عند بلوغ الحد أو فشل الخدمة.
  - تسجيل تشخيصي آمن لا يحتوي أسرارًا أو نصوصًا حساسة كاملة.
- **التحقق المطلوب:** اختبارات تجاوز الحدود وطلبات متزامنة.

### 1.1) حدود JSON لمدخلات Gemini الخمسة الرئيسية
- **الحالة:** تمّت بنجاح في الكود — 2026-09-04
- **المشكلة:** مسارات `plan`، `evaluate`، `investigate`، `ai-edit`، `daily-focus` كانت تقبل JSON بلا حد للحجم وتمرره مباشرةً لمولّد prompt لـ Gemini، مما يسمح بالتكلفة وتجاوز حدود المزوّد.
- **التنفيذ:** أضيف `src/lib/request-limits.ts` بدالتين: فحص مبكر لـ `Content-Length` قبل قراءة البادي، وفتحص لاحق لحجم JSON المحلّل للطلبات بلا `Content-Length` موثوق. الحد الحالي 200KB لكل طلب. يرتبط الفحص قبل استهلاك quota وقبل الاتصال بGemini.
- **الملفات المتأثرة:** `src/lib/request-limits.ts`، `src/app/api/goal/{plan,evaluate,investigate,ai-edit,daily-focus}/route.ts`
- **التحقق:** تم تشغيل `npx tsc --noEmit` بنجاح.
- **متبقي:** هذا سقف تريبط خام، وليس schema تفصيلي لكل حقل، وهو كافٍِ لمنع الإساءة الفاقعة وليس لضبط تكلفة tokens بدقة.

### 1.2) معالجة سباقات create/join challenge وتقوية رمز الدعوة
- **الحالة:** تمّت بنجاح في الكود — 2026-09-04. يحتاج تطبيق يدوي على Supabase (راجع الملاحظة أدناه).
- **المشكلة:** `create_goal_challenge` و`join_goal_challenge` كانت تفحص الشرط ثم تنفّذ الإدراج بدون قفل؛ طلبان متوازيان من نفس المستخدم (create) أو لنفس الروم (join) قد يتجاوزان فحص الشرط معًا ويكسران قيد تحدي واحد نشط/مشاركين اثنين للروم. رمز الدعوة كان 6 محارف فقط (~30 بت) بلا حد محاولات.
- **التنفيذ:** migration `0008_challenge_hardening.sql` تضيف:
  - `pg_advisory_xact_lock` مفتاحي بمفتاح `challenge_create:<user_id>` داخل `create_goal_challenge` قبل فحص وإدراج التحدي النشط.
  - `pg_advisory_xact_lock` مفتاحي بمفتاح `challenge_join:<room_id>` داخل `join_goal_challenge` قبل فحص وإدراج عدد المشاركين.
  - رمز الدعوة من 6 إلى 10 محارف (من ~30 بت إلى ~50 بت) في `metrix_new_invite_code`.
- **الملفات المتأثرة:** `supabase/migrations/0008_challenge_hardening.sql`، `supabase/setup.sql`
- **شرط الإطلاق — يحتاج إجراءًا يدويًا:** مثل migration `0007`، لا يوجد اتصال Postgres مباشر متاح لتطبيقه الآن. يجب لصق `supabase/migrations/0008_challenge_hardening.sql` في Supabase SQL Editor وتشفيله قبل اعتبار هذه الحماية فعّالة في الإنتاج.

### 2.1) حجز الحصة الذري لخدمات AI
- **الحالة:** تمّت بنجاح في الكود — 2026-09-04
- **التنفيذ:** أضيف سجل استخدام `ai_usage_events` ودالة RPC ذرية `consume_ai_quota` في migration `0007`. الدالة تقفل حصة المستخدم والمزوّد داخل المعاملة، وتفرض حدًا متحركًا لكل دقيقة وحدًا يوميًا UTC قبل الاتصال بالمزوّد الخارجي.
- **الحدود الحالية:** Gemini: 6 طلبات/دقيقة و30 طلبًا/يوم لكل مستخدم. Mistral transcription: 3 طلبات/دقيقة و10 طلبات/يوم لكل مستخدم.
- **ربط المسارات:** plan، evaluate، investigate، ai-edit، daily-focus، task-mini، weekly-review، milestone evaluation، وtranscribe.
- **حماية الصوت:** سقف 10MB، allowlist لصيغ الصوت، تحقق `File`، وتطبيع اللغة إلى `ar` أو `en`. لم تعد استجابة العميل تكشف body خطأ Mistral الخام.
- **الملفات المتأثرة:**
  - `supabase/migrations/0007_ai_usage_limits.sql`
  - `supabase/setup.sql`
  - `src/lib/ai-quota.ts`
  - `src/app/api/goal/{plan,evaluate,investigate,ai-edit,daily-focus,task-mini,weekly-review,milestone}/route.ts`
  - `src/app/api/transcribe/route.ts`
- **التحقق:** تم تشغيل `npx tsc --noEmit` بنجاح.
- **شرط الإطلاق — يحتاج إجراءًا يدويًا من المالك:** `migration 0007_ai_usage_limits.sql` **لم يُطبّق بعد على Supabase الحي**. لا توجد لدي صلاحية اتصال مباشر بPostgres (لا `DATABASE_URL` ولا Supabase CLI مرتبط) وREST/PostgREST لا يقبل الDDL الخام. المطلوب منك أحد الخيارين:
  1. لصق محتوى `supabase/migrations/0007_ai_usage_limits.sql` يدويًا في Supabase Dashboard → SQL Editor وتشفيله.
  2. أو منحي رابط اتصال Postgres مباشر (Session pooler `postgres://...`) لأشقّه عبر `psql` أو Supabase CLI.
  بدون هذا، مسارات AI التي ربطتها بÅ€`requireAiQuota` سترجع `503 ai_quota_unavailable` لكل المستخدمين في الإنتاج الحالي حتى يُنفّذ أحد الخيارين أعلاه.

### 3) التحقق من قاعدة بيانات Supabase في بيئة الإنتاج
- **الحالة:** تمّت بنجاح للschema الحالية (0001–0006) — 2026-09-04. **ملاحظة:** هذا لا يشمل migration `0007` الجديدة الموصوفة في المهمة 2.1، وتلك تبقى مفتوحة تحتاج التدخل اليدوي الموصوف أعلاه.
- **الهدف:** التأكد من تطبيق schema والمهاجرات وRLS وRPCs الفعلية.
- **النطاق:** `supabase/setup.sql`، `supabase/migrations/`، `scripts/check-db.sh`
- **التحقق المنفذ:** تشفيل `./scripts/check-db.sh` على المشروع الحي الموصول في `.env.local`. النتيجة:
  ```
  == tables ==            ALL ok (8/8)
  == rpc functions ==     ALL ok (6/6)
  == rpc contracts ==     ALL ok
  == rls helpers (0005) == ok
  == 0006 adaptive columns == ALL ok (بما فيها weekly_reviews)
  == storage buckets ==   ok avatars, ok milestones
  ALL CHECKS PASSED
  ```
- **المتبقي:** اختبار منع recursion والتداخل بين حسابين يتم ضمن المهمة 1 (التدقيق اليدوي للAPI)، وات مرتبط بتطبيق migration 0007 أعلاه.

### 4) نجاح بناء Production
- **الحالة:** تمّت بنجاح — 2026-09-04
- **التنفيذ:** تم حذف `.next` وإعادة `npm run build` من الصفر بمهلة أطول.
- **النتيجة:** اكتمل البناء بنجاح في نحو 20 ثانية إجماليًا (`Compiled successfully in 16.5s`، `Finished TypeScript in 3.2s`)، وتم توليد جميع مسارات API (17 مسارًا) والصفحات الثابتة بلا أخطاء.
- **ملاحظة:** البطء السابق كان مجرد بطء طبيعي لمهلة الاختبار، وليس مشكلة build دائمة.
- **المتبقي:** `npm run start` لم يُتحقق منه لأنه يشعّل سيرفر مستمرًا (ممنوع في بيئة هذه المهمة)؛ موصوف كخطوة تحقق يدوية قبل النشر.

### 5) مراقبة الأخطاء والسجلات الآمنة
- **الحالة:** قيد التنفيذ — تم تنفيذ بند 5.1 أدناه؛ بقي Error tracking خدمة خارجية تحتاج قرار المستخدم (مثل Sentry) ومفاتيح حساب.

### 5.1) تنظيف السجلات التي تكشف مدخلات المستخدم
- **الحالة:** تمّت بنجاح — 2026-09-04
- **المشكلة:** المسار `investigate` كان يسجّل محتوى الهدف والسياق الكامل في السيرفر، والواجهة كانت تسجّل نص الفريق المفرّغ صوتيًا في console المتصفح.
- **التنفيذ:**
  - حذف `console.log` الذي كان يسجّل `goal`/`context` في `src/app/api/goal/investigate/route.ts` (ضمن دمج الحماية في المهمة 2.1).
  - حذف `console.log`/`console.warn` المتسلة لنص التفريف الكامل من `src/components/goal/GoalCreator.tsx` و`src/components/shared/VoiceRecorder.tsx`، والاقتصار على تسجيل رمز التجاوب عند الفشل.
  - توقف `src/app/api/transcribe/route.ts` عن إرجاع body خطأ Mistral الخام للعميل (ضمن المهمة 2.1).
- **المتبقي:** رسالات `console.error(error)` المتبقية في مسارات API تسجّل كائن الخطأ للتشخيص الداخلي فقط، وليست مدخلات مستخدم خامة؛ مقبولة حاليًا ولا تحتاج تغييرًا فوريًا، لكن يفضل ربطها بخدمة error tracking مركزية لاحقًا (المهمة 5).
- **الملفات المتأثرة:**
  - `src/app/api/goal/investigate/route.ts`
  - `src/components/goal/GoalCreator.tsx`
  - `src/components/shared/VoiceRecorder.tsx`
  - `src/app/api/transcribe/route.ts`
- **التحقق:** تم تشغيل `npx tsc --noEmit` بنجاح.
- **الهدف:** رصد أخطاء الإنتاج وفشل OAuth وSupabase وAI دون تسريب بيانات.
- **معايير القبول:**
  - Error tracking مفعّل (مثل Sentry أو بديل مناسب).
  - Request/build identifiers في السجلات.
  - تنبيهات 5xx وفشل خدمات AI.
  - إزالة أو استبدال `console.log` الإنتاجية التي تسجل مدخلات المستخدم.
- **ملفات تحتاج مراجعة:**
  - `src/app/api/goal/investigate/route.ts`
  - `src/components/goal/GoalCreator.tsx`
  - `src/components/shared/VoiceRecorder.tsx`
  - `src/lib/gemini.ts`

### 6) الخصوصية وحذف البيانات
- **الحالة:** نسخة مبدئية تمّت بنجاح في المنتج — 2026-09-04. **تحذير:** هذا نص قانوني مسودة الأولى وليس مراجعًا من محام؛ يجب على المالك مراجعته قانونيًا قبل الإطلاق الفعلي.
- **التنفيذ:**
  - أنشأت صفحتي `src/app/privacy/page.tsx` و`src/app/terms/page.tsx` (ثنائية اللفة، تقرأ للزائر المتصفح أو غير المسجّل).
  - توضح Privacy Policy: ما البيانات المجموعة، استخدام Gemini/Mistral، موقع التخزين (Supabase)، الاحتفاظ بالصوت/الصور، ومسار طلب حذف الحساب عبر البريد.
  - أضيف `/privacy` و`/terms` للمسارات العامة في `src/proxy.ts` ليراهما الزائر المستخدم والمستخدم المسجّل معًا بدون رد إلى `/login`.
  - ربط روابط `Terms of Service` و`Privacy Policy` الفعلية في نص `/login` الموجود مسبقًا (كان نصًا بلا روابط).
- **الملفات المتأثرة:** `src/app/privacy/page.tsx`، `src/app/terms/page.tsx`، `src/proxy.ts`، `src/app/login/page.tsx`
- **التحقق:** `npx tsc --noEmit` و`npm run build` نجحا معًا، ويظهر `/privacy` و`/terms` كمسارين ثابتين في مخرجات البناء.
- **متبقي لم يكتمل:** مراجعة قانونية للنص، وتحديد مسار حذف حساب منفذ فعليًا (داخل التطبيق لا عبر البريد فقط) إن أراد المالك ذلك لاحقًا.

---

## P1 — مطلوبة قبل Public Beta

### 7) اختبارات للمنطق الحساس
- **الحالة:** تمّت بنجاح جزئيًا — 2026-09-04 (المنطق التالت المنطق التالي: `task-periods.ts`، `streak.ts`، `task-hierarchy.ts`. `api-auth.ts` يتم اختباره فعليًا عبر المهمة 1 وليس بالوحدات).
- **لم يوجد أي runner اختبارات في المشروع وفشل `npm install` لإضافة vitest بسبب مشكلة npm cache مملوكة لroot محليًا. لم أضف dependency جديدة واستخدمت بدلًا `node --test` المدمت في Node 24.
- **التنفيذ:**
  - أضيف `scripts/test-alias-loader.mjs`: موفّر ESM صريح يحل alias `@/*` إلى `src/*` لـ `node --test`، لان Node الخام لا يقرأ `tsconfig.paths`.
  - أضيف `"test"` script في `package.json` يشفّل `node --test` على `src/**/__tests__/*.test.ts`.
  - فعّل `allowImportingTsExtensions` في `tsconfig.json` (مقبول لأن `noEmit: true` مفعل فعليًا) ليقبل `tsc` الاستيراد بامتداد `.ts` المطلوب لتشفيل Node المباشر.
  - أضيف 27 اختبارًا في `src/lib/__tests__/`: `task-periods.test.ts` (8)، `streak.test.ts` (11)، `task-hierarchy.test.ts` (8).
- **النطاق المتبقي المدون:** `src/lib/api-auth.ts` (يحتاج مختبرات تكاملية بحسابين حقيقيين وليس unit test).
- **التحقق المنفذ:** `npm test` → 27/27 نجحت. `npx tsc --noEmit` → نجح. `npm run build` → نجح (ملفات الاختبار لا تؤثر على مسارات Next لأنها داخل `src/lib`، لا `src/app`).

### 8) إكمال تفاعل الإشعارات
- **الحالة:** تمّت بنجاح — 2026-09-04
- **المشكلة:** الضفط على الإشعار كان يسجل فقط في console ولا ينفّذ أي إجراء.
- **التنفيذ:** أضيف `onNotificationClick` إلى `NotificationsSection`، وربطه في `HomePage.tsx` بفتح لوحة الهدف الأساسي المرتبط بالإشعار عبر `onSelectGoal(primaryGoal.id)` الموجود فعليًا. لم يتطلب إضافة prop تنقل عبر `page.tsx`.
- **الملفات المتأثرة:** `src/components/notifications/NotificationsSection.tsx`، `src/components/HomePage.tsx`
- **التحقق:** تم تشغيل `npx tsc --noEmit` بنجاح. النقر الان يفتح لوحة الهدف بدلاً من أن يكون بلا أي فعل.

### 8.1) إصلاح تفويض التقرير الأسبوعي
- **الحالة:** تمّت بنجاح — 2026-09-04
- **المشكلة:** كان `weekly-review` يجلب الهدف باستخدام `goalId` فقط. صلاحيات RLS الخاصة بالتحديات تسمح للمنافس بقراءة بعض بيانات هدف خصمه، وبالتالي كان يستطيع طلب تجميع سلوكي تفصيلي ونتيجة AI للخصم.
- **التنفيذ:** أضيف قيد ملكية صريح `user_id = auth.user.id` إلى استعلام الهدف.
- **الملف المتأثر:** `src/app/api/goal/weekly-review/route.ts`
- **التحقق:** مراجعة ساكنة للاستعلام؛ حساب غير مالك الهدف سيحصل الآن على `404 Goal not found` قبل تحميل check-ins/logs أو استدعاء Gemini. تم تشغيل `npx tsc --noEmit` بنجاح. يلزم اختبار تكاملي بحسابين ضمن مهمة API الشاملة.

### 8.2) إصلاح تفويض إنشاء تذكيرات الهدف
- **الحالة:** تمّت بنجاح — 2026-09-04
- **المشكلة:** كان مسار إنشاء التذكير يربط `goal_id` القادم من العميل بتذكير المستخدم بدون التأكد أن الهدف مملوك له.
- **التنفيذ:** أضيف استعلام ملكية صريح للهدف (`id` و`user_id`) قبل الإدراج؛ الطلب غير المصرح به ينتهي بـ`404` ولا ينشئ تذكيرًا.
- **الملف المتأثر:** `src/app/api/goal-reminders/route.ts`
- **التحقق:** تم تشغيل `npx tsc --noEmit` بنجاح بعد التعديل. يلزم اختبار تكاملي بحسابين ضمن مهمة API الشاملة.

### 9) حالات التحميل والخطأ وعدم وجود بيانات
- **الحالة:** فحص موضعي تم في 2026-09-04، المهمة ليست مكتملة.
- **ما تم فعليًا:** فحص أعلى خطر مسار (إنشاء الهدف في `src/components/goal/GoalCreator.tsx`) ووجد أنه يطبّق فعليًا disabled/loading صحيحة لمنع double-submit (`disabled={saving}`، `disabled={!goalText.trim() || loading}`، منع التسجيل المزدوج عبر `isProcessingAudio`).
- **ما لم يتم:** تدقيق مماثل لكل مسارات المنتج الأخرى (milestones، challenges، المراجعة الأسبوعية، التذكيرات) لم يُنفّذ بسبب اتساع نطاق الواجهة. هذا يحتاج mapping تفصيلي لكل مكوّن async ثم مراجعة واحدة واحدة.
- **الهدف:** كل عملية async مهمة تعرض loading/error/retry وتمنع double submit.
- **النطاق المتبقي:** إنشاء goal (تم التحقق منه)، AI plan، التقييم، milestones، الصوت، challenges، الصور، الجلسة.

### 10) اختبار Web وAndroid يدويًا
- **الحالة:** محجوب — يتطلب أجهزة/متاصفحات وتفاعل بشريًا حقيقيًا (مس shake، تسجيل صوت، إذن، إرسال OAuth حقيقي)، لا يمكن تنفيذه تلقائيًا من هنا.
- **التحقق المطلوب منك:**
  - Web: Chrome، Safari iOS، Chrome Android، شاشات صفيرة، اتصال ضعيف، RTL وLTR.
  - Auth: تسجيل الدخول/الخروج، refresh، OAuth callback.
  - Android: deep links، العودة من OAuth، الصلاحيات، keyboard RTL، back button، offline/resume، signed release.

### 11) تحديث وثائق المشروع وبيئة التطوير
- **الحالة:** تمّت بنجاح — 2026-09-04
- **المشكلات المرصودة سابقًا:** `README.md` لا يعكس الواقع (العربية والموبايل ما زالت في roadmap)، اسم الحزمة `my-app`، ولا يوجد `.env.example`.
- **التنفيذ:**
  - أعيد كتابة `README.md` بالعربية ليعكس الحالة الدقيقة: دعم العربية/الموبايل فعلي، الأوامر الفعلية (`check-db.sh`، `build-mobile.sh`)، وربط مباشر إلى `task.md` لحالة التجهيز.
  - أنشأت `package.json` → `name: "metrix"` بدليل `my-app`.
  - أنشأت `.env.example` بكل متغيرات البيئة المذكورة في `CLAUDE.md` بدون قيم حقيقية.
- **الملفات المتأثرة:** `README.md`، `package.json`، `.env.example`
- **التحقق:** مراجعة يدوية للمحتوى. لم يتم وضع أي قيمة سرية في `.env.example`.

---

## P2 — تحسينات بعد الإطلاق الأول

### 12) تقليل مركزية الحالة في `page.tsx`
- **الحالة:** مؤجل عمدًا — مصنف رسميًا كـ P2 "بعد الإطلاق الأول" لأنه إعادة هيكلة واسعة المخاطر وليست مانعًا للإطلاق؛ لم تُنفّذ في هذه الدفعة لتجنّب التعارض مع بقية التعديلات المـوقّتة.
- **الهدف:** فصل حالات goals/navigation/language/auth إلى hooks أو وحدات مستقلة بصورة تدريجية، بدون إعادة كتابة كبيرة.

### 13) توسيع تفطية الاختبارات وE2E
- **الحالة:** مؤجل عمدًا — قاعدة الاختبار و27 اختبارًا للمنطق الحاسم تمّت في المهمة 7؛ هذه المهمة تخص E2E لمسارات مستخدم كاملة (متصفح + قاعدة حقيقية)، وهي من طبيعتها بعد الإطلاق الأول.
- **الهدف:** تفطية مسارات المستخدم الحرجة: OAuth، goal creation، check-ins، milestones، challenges، mobile auth.

### 14) قياس الأداء والتحليلات
- **الحالة:** لم يبدأ — يتطلب قرار منتج للأداة (Vercel Analytics، PostHog، غيرها) قبل التنفيذ.
- **الهدف:** قياس Core Web Vitals، أوقات AI، نسب الفشل، وإكمال مسارات المستخدم دون جمع بيانات أكثر من اللازم.

---

## مكتملة

- [x] إنشاء سجل مهام إنتاج حيّ في `task.md`.
  - **التاريخ:** 2026-09-04
  - **التحقق:** تم إنشاء الملف بنجاح.

- [x] فحص تشخيصات المشروع عبر Zed.
  - **التاريخ:** 2026-09-04
  - **النتيجة:** لا توجد diagnostics حالية.
  - **ملاحظة:** هذا لا يغني عن `tsc` وbuild واختبارات الإنتاج.

---

## سجل التنفيذ

### 2026-09-04 — تدقيق API، الدفعة الأولى

- راجعت مسارات AI، challenges، والتذكيرات مع طبقة المصادقة وRLS المصدرية.
- **نتائج سليمة:** لم يظهر مسار API مكشوف بلا مصادقة في المراجعة الساكنة؛ المسارات تستخدم `requireUser` أو تحققًا مباشرًا عبر `auth.getUser()`.
- **إصلاحات مكتملة:** عزل تقرير الأسبوع عن أهداف المنافس، منع إنشاء تذكير مرتبط بهدف غير مملوك للمستخدم، وإضافة quota ذرية لخدمات AI مع تحصين رفع الصوت.
- **نتائج متبقية يجب علاجها:** تطبيق migration 0007 واختبارها على Supabase، وضع حدود schema لبقية أجسام JSON الخاصة بـ AI، وعمليات التحديات تحتاج معالجة سباقات الإنشاء/الانضمام في SQL.
- **التحقق المنفذ:** `npx tsc --noEmit` — نجح.

### 2026-09-04 — الدفعة التالية: quota، تحديد JSON، سباقات challenges، اختبارات، وخصوصية

- طبّقت توصيات مراجعة sub-agent لتركيب دالة quota ذرية في Postgres (`ai_usage_events` + `consume_ai_quota`) وربطتها بتسعة مسارات Gemini/Mistral.
- أضفت حدود حجم JSON (`src/lib/request-limits.ts`) لمسارات plan/evaluate/investigate/ai-edit/daily-focus قبل استهلاك quota.
- أصلحت سباقات create/join challenge ورفعت قوة رمز الدعوة عبر migration `0008`.
- أضفت 27 اختبارًا للمنطق الحاسم باستخدام `node --test` المدمج، بلا dependencies خارجية (فشل `npm install` بسبب ملكية npm cache معطلة).
- نظفت سجلات المتصفح والسيرفر التي تكشف مدخلات المستخدم، وأكملت تفاعل الإشعارات.
- أنشأت مسودة `/privacy` و`/terms` وربطتهما في `/login` و`src/proxy.ts`.
- حدّثت `README.md`، `package.json`، وأضفت `.env.example`.
- أعدت تشفيل `npm run build` من الصفر بمهلة أطول فنجح فعليًا (البطء السابق كان مهلة الأداة فقط).
- **التحقق المنفذ لهذه الدفعة:** `npm test` (27/27)، `npx tsc --noEmit`، `npm run build` — الثلاثة نجحت معًا في تشفيلة واحدة.
- **المتبقي الوحيد المرتبط بقرار يدوي خارج الكود:** لصق migrations `0007` و`0008` في Supabase SQL Editor لمشروع Supabase الحي الموصوف في `.env.local`.

## المخاطر والقرارات المفتوحة

- لا توجد Test suite كاملة للمنطق بأكمله; الاعتماد الحالي على `node --test` يفتقر لواجهة UI/hooks/API integration.
- `npm run lint` يملك أخطاء وتحذيرات سابقة بحسب `CLAUDE.md`، لذلك يجب تحديد baseline قبل اعتبار أي lint failure جديدًا.
- بناء Production يحتاج إعادة تشغيل بمهلة أطول لمعرفة هل البطء مؤقت أم توجد مشكلة build فعلية.
- لا يجوز نشر مفاتيح `SUPABASE_SERVICE_ROLE_KEY` أو `GEMINI_API_KEY` أو `MISTRAL_API_KEY` في العميل أو السجلات.
